"use client";
import { motion } from "motion/react";
const steps = [["01", "Understand", "The brief, audience and problem."],["02", "Explore", "Concepts, references and visual directions."],["03", "Shape", "The strongest idea becomes a system."],["04", "Build", "Identity, campaigns, packaging and everything around it."],["05", "Deliver", "Work that is ready for the real world."]];
export default function Process(){return <section className="process section-pad"><div className="section-head compact"><div><p className="eyebrow">06 / Process</p><h2>How an idea<br /><em>takes shape.</em></h2></div><p className="section-intro">A straightforward process with enough room for exploration and enough structure to keep the work moving.</p></div><div className="process-grid">{steps.map(([n,t,c],i)=><motion.div key={n} className="process-card" initial={{ opacity:0, y:50 }} whileInView={{ opacity:1,y:0 }} viewport={{once:true,amount:.3}} transition={{delay:i*.08}}><span>{n}</span><h3>{t}</h3><p>{c}</p></motion.div>)}</div></section>}
